extends layout 

block content 
  form(method="post")
    label(for="url") Enter URL to shorten 
    input(type="text", name="url" id="url" required value=urls)
    input(type="submit", value="Shorten URL →")
