extends layout 

include mixins/_short-links-table
include mixins/_meta-tags

block meta
  +metaTags(url, description)

block content
  h3.title List of the shortened link
  p ⚠️ #[span.warning Note : ] This page will only be available for 24 hours (links expire as configured)
  if urlIds && urlIds.length 
    +table(urlIds)   
    .small-screen
      p 👆 Scroll right if you can't reach shortened URL
  else 
    h4 Oops! Seems like the links expired already!

